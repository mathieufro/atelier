// `part` prop is accepted for caller compatibility but not used internally
export function CompactionPartView(_props: { part?: unknown }) {
  return (
    <div class="text-sm text-vsc-disabled-fg italic">
      Context compacted
    </div>
  )
}
